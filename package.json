{
  "name": "dress-deal-tracker",
  "version": "1.0.0",
  "description": "Lightweight dress deal tracker for women's clothing from Myntra, Meesho, and Amazon",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "test": "node --test tests/*.test.js",
    "collect": "node src/collectors/run.js",
    "check-deals": "node src/deals/check.js"
  },
  "dependencies": {
    "wrangler": "^3.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
